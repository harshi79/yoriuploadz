{
  "name": "yoriupload",
  "version": "3.0.0",
  "private": true,
  "description": "Permanent Catbox file sharing on a simple Render web service.",
  "scripts": {
    "dev": "node --env-file-if-exists=.env server.js",
    "build": "npm run check",
    "check": "node --check public/app.js && node --check server.js && npm test",
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=22"
  }
}
