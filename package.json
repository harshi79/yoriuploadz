{
  "name": "yoriupload",
  "version": "2.1.0",
  "private": true,
  "description": "Permanent Catbox file sharing through a protected Cloudflare Worker.",
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0 --port 8888",
    "build": "npm run check && wrangler deploy --dry-run",
    "check": "node --check public/app.js && node --check scripts/dev-server.js && node --check worker/index.mjs && node --test tests/*.test.mjs",
    "dev:static": "node scripts/dev-server.js",
    "deploy": "wrangler deploy",
    "cf:login": "wrangler login",
    "cf:secret": "wrangler secret put CATBOX_USERHASH"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  }
}
