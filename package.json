{
  "name": "yoriupload",
  "version": "2.0.0",
  "private": true,
  "description": "A focused, account-free file sharing interface.",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "dev:netlify": "npx netlify-cli dev",
    "build": "npm run check",
    "check": "node --check public/app.js && node --check scripts/dev-server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
