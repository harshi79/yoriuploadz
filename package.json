{
  "name": "yoriupload",
  "version": "1.0.0",
  "private": true,
  "description": "YoriUpload — fast, private file sharing.",
  "scripts": {
    "dev": "npx netlify-cli dev",
    "dev:local": "node scripts/dev-server.js",
    "dev:offline": "MOCK_UPSTREAM=1 node scripts/dev-server.js",
    "build": "echo 'static frontend, nothing to build'"
  },
  "engines": {
    "node": ">=18"
  }
}
