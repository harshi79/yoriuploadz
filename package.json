{
  "name": "yoriupload",
  "version": "3.1.0",
  "private": true,
  "description": "Dockerized permanent Catbox sharing with gated Yori downloads.",
  "scripts": {
    "dev": "node --env-file-if-exists=.env server.js",
    "build": "npm run check",
    "check": "node --check public/app.js && node --check server.js && npm test",
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=22"
  }
}
